<br/>
<br/>
<br/>
<br/>
<body>

<div id="full">
<link href='/assets/feedstyle.css' type='text/css' rel='stylesheet'>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
<script>
      
            $('#sea').keyup(function(){
               
                var entered = {qry: $('#sea').val()};
                $.ajax({
                    type:'POST',
                    url:'/search',
                    data: entered,
                    success:function(result){
                        $("#full").html('');
                      for(var i=0;i<result.length;i++){
                          var str = "<div id='question'>"+ result[i].name + "</div>" + "<div id='answer'>" 
                                    + result[i].answers[0].ans + "</div>" + "<hr>";
                            $("#full").append(str);
                          }
                          document.location.reload;
                          console.log("after ");
                    }
                });
                return false;
            });
    
        </script>
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">

<!-- <% for ( var i=0;i<arr.length;i++) { %>
    
    
    <div id="question">
    <%= arr[i].name %>
    </div>
   
    <div id="answer">
    <%= arr[i].answers[0].ans %>
    </div>

    <button style="margin-left:10px">Like</button>
    <br/>
    <hr>

<% } %> -->
</div>
</body>
<form method="POST" action="/home">
    <button type="submit" id="uname" >Logout</button>
  </form>
  <% include ./nav.ejs %>
<input type="text" name="search" id="sea" placeholder="Search for user" class="form-control"></input>
<script>
$(document).ready(function(){
    $('#sea').keyup(function(){
       
        var entered = {qry: $('#sea').val()};
        $.ajax({
            type:'POST',
            url:'/search',
            data: entered,
            success:function(result){
                $("#full").html('');
              for(var i=0;i<result.length;i++){
                  var str = "<div id='question'>"+ result[i].name + "</div>" + "<div id='answer'>" 
                            + result[i].answers[0].ans + "</div>" + "<hr>";
                    $("#full").append(str);
                  }
            }
        });
        return false;
    });
});
</script>
